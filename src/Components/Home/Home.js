import React from 'react';
import { Container } from 'react-bootstrap';
import Banner from './Banner';

const Home = () => {
    return (
        <Container fluid>
            <Banner />
        </Container>
    );
};

export default Home;
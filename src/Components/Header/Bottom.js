import React from 'react';
import { Container } from 'react-bootstrap';

const Bottom = () => {
    return (
        <Container fluid>
            this is mega menu
        </Container>
    );
};

export default Bottom;